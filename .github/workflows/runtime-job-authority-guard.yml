name: Runtime Job Authority Guard Verification

on:
  pull_request:
    branches:
      - spike/agent-job-binding-v1
    paths:
      - 'backend/app/agent_runtime/job_bound_runtime.py'
      - 'backend/tests/agent_runtime/test_job_bound_runtime_authority.py'
      - '.github/workflows/runtime-job-authority-guard.yml'
  workflow_dispatch:

env:
  PYTHONPATH: .

jobs:
  focused:
    name: Runtime Job authority boundary tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install project dependencies
        run: |
          python - <<'PY'
          import subprocess, sys, tomllib
          with open('pyproject.toml', 'rb') as handle:
              project = tomllib.load(handle)
          deps = project['project']['dependencies'] + project['project']['optional-dependencies']['dev']
          subprocess.check_call([sys.executable, '-m', 'pip', 'install', *deps])
          PY
      - name: Compile Job-bound runtime slice
        run: python -m compileall -q backend/app/agent_runtime/job_bound_runtime.py backend/tests/agent_runtime/test_job_bound_runtime_authority.py
      - name: Run focused Runtime Job authority tests
        run: python -m pytest backend/tests/agent_runtime/test_job_bound_runtime_authority.py -q
      - name: Run complete Agent Runtime acceptance
        run: python -m pytest backend/tests/agent_runtime -q
      - name: Run existing Job state-machine tests
        run: python -m pytest backend/tests/test_job_state_machine.py backend/tests/test_job_batch.py -q
