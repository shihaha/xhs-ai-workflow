# SDD ledger — plan: docs/superpowers/plans/2026-08-18-bailian-visual-image-generation.md

## Preflight rulings

| Interface | Ruling |
|---|---|
| Text / vision / image configuration | Separate trusted Settings fields and models; only the API key is shared. Request contracts expose no endpoint, model, URL or output path. |
| Vision output | Strict advisory `VisualAssessment` only. It has no review/approval mutation capability. |
| Generated image output | Return bytes only after MIME agreement, supported-format decode, full load, dimensions, pixels, bytes and digest are proven. |
| Bailian asynchronous image task | Fixed service route and configured model, bounded HTTP retries, terminal-status allowlist, polling deadline and poll-count cap. |
| Provider facts | Store only model, bounded usage, request/task IDs, prompt version and sanitized attempt categories; never provider bodies, headers or API keys. |
| Live validation | The first opt-in generation attempt on 2026-08-19 reached Bailian and truthfully failed at submission with HTTP 400 `InvalidParameter` / `url error`; the provider request ID was retained in local live output. The cause was the client's obsolete wan2.5-and-earlier route/body. Vision was not reached and no live success was recorded. |

Task 1: compatibility fix implemented; live rerun pending. The first real wan2.6-t2i gate exposed the obsolete `/services/aigc/text2image/image-synthesis` + `input.prompt` contract. A strict MockTransport RED reproduced the wrong route. The client now submits to `/services/aigc/image-generation/generation` with `input.messages[].content[].text` and reads the single generated URL from `output.choices[].message.content[].image`, while retaining the existing bounded poll, credential-free download and full byte validation. Focused GREEN: target 1 passed; media 57 passed. A full backend run from a clean parent cwd reached 1361 passed/3 skipped with one unrelated cleanup-worker child failure: that child deliberately changes cwd back to the repository and therefore loaded the live `.env`'s external `XHS_CLI_STATE_DIR`, which conflicts with its temporary runtime. No `.env` value was printed or modified. Real Bailian success remains unproven until the parent reruns the opt-in gate.

Task 2: implementation and review fix round 1 complete; focused rereview pending. TDD RED covered the missing media persistence package, terminal/read-schema behaviors and the review-proven raw failure-text persistence. Failure transitions now map only a fixed safe category allowlist and fixed sanitized detail; caller exception/category text is never stored. Latest focused/necessary backend verification: 45 passed. Earlier full backend regression: 1338 passed, 2 guarded live skips; no failures. Live Bailian remains not_run.

Task 3: implementation and review fix round 1 complete; focused rereview pending. Generated bytes now persist only as server-named managed `output_image` materials after a durable cleanup reservation. Material, generated provenance, cleanup cancellation, media run and reserved job succeed together, with exact commit-ack proof and retained cleanup on uncertain failure. Visual assessments are durable, sealed advisory artifacts bound to the exact run/revision/material bytes and physical identities; reads fail closed on content, file or assessment drift and never change review/content approval state. Review fix round 1 also requires the provider-returned `VisionResult.model` to equal the model reserved on the run before any assessment is persisted. Latest focused visual verification: 7 passed. Media + content regression: 346 passed. Full backend regression: 1352 passed, 2 guarded live skips. Live Bailian remains not_run.

Task 4: implementation and review fix round 1 complete; bounded rereview pending. TDD RED was the expected missing media API/worker import failure. The app now owns one serialized durable media worker with startup recovery and shutdown admission fencing; strict submission accepts only trusted IDs, generic Jobs cannot mutate reserved media work, and list/detail plus separate media health facts are wired through the shared service. Review fix round 1 also fail-closes every pre-existing running run to needs_human at fresh worker startup, including future leases, while queued work resumes and post-start claims are unaffected. Final focused verification: 96 passed. Full backend regression: 1361 passed, 2 guarded live skips; no failures. Live Bailian remains not_run.

Task 5: implementation complete. Content Studio now submits and polls durable generation/analysis runs, shows truthful provider/model/usage/duration/error/output facts, supports explicit retry, and keeps advisory vision separate from manual approval. Controlled E2E now passes real PNG bytes through API -> worker -> DB -> managed file/material -> UI -> advisory vision -> explicit human review -> available ZIP, with no prewritten cover placeholder. Final verification: frontend 51 tests and build passed; controlled E2E repeat5 passed; focused media/backend 70 passed with one guarded live skip; full backend 1362 passed with 3 guarded/platform skips; release tests/scans and dependency audit passed. Real Bailian media remains not_run pending explicit local opt-in/key/models.
